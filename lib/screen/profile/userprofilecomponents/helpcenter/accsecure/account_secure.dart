import 'package:flutter/material.dart';

class AccountSecureMenu extends StatelessWidget {
  const AccountSecureMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
