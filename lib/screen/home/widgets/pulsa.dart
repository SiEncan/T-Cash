import 'package:flutter/material.dart';

class Pulsa extends StatelessWidget {
  const Pulsa({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: const Color.fromARGB(255, 14, 127, 255),
      ),
    );
  }
}
